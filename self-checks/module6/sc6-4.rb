quiz "6.4: The Document Object Model (DOM) and jQuery" do
	choice_answer do
		text "When using JavaScript for client-side form validation, which is NOT true?"
		answer "The server doesn't have to repeat validations already performed by JavaScript"
		distractor "JavaScript code can inspect DOM element attributes to see what the user typed"
		distractor "JavaScript code can prevent the "Submit" button from submitting the form"
		distractor "Some validations may be impractical to perform on client so must be done on server"
		explanation "As described in the answers"
	end
end