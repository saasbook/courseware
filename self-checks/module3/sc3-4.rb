quiz "3.4: From Web Sites to Microservices: Service-Oriented Architecture" do
	choice_answer do
		text "Match the terms: (a) presentation tier, (b) logic tier, (c) persistence tier"
		answer "(a) Apache web server (b) Rack+Rails (c) database"
		distractor "(a) Firefox (b) Apache web server (c) PostgreSQL"
		distractor "(a) Microsoft Internet Information Server (b) Rack+Rails (c) Apache web server"
		distractor "(a) Firefox (b) Microsoft Internet Information Server (c) MySQL"
		explanation "Recall that the presentation tier refers to the front end layer that presents the user interface, the logic tier contains the core capabilities, and the data tier is for storage."
	end
end