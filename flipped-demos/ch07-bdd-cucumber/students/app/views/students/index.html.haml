%h1 All Students

%table
  %thead
    %tr
      %th ID num
      %th Last
      %th First
  %tbody
    - @students.each do |student|
      %tr
        %td= student.sid_number
        %td= student.last_name
        %td= student.first_name

